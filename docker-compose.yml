version: "3.7"

services:
  myapitemplate2:
    build:
      context: ./
      dockerfile: release.Dockerfile
    container_name: DNAC-Comp-Monitor
    restart: always
    env_file:
      - app/dev.env
    volumes:
      - "./app:/app"
    ports:
      - "8080:8080"
